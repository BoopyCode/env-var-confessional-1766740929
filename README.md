# Env Var Confessional

A simple tool that checks your environment variables against a template file (.env.example) before runtime. Prevents those "works on my machine" moments by catching missing variables early. Run: `node env-confessional.js`